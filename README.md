# 🎬 Telegram Бот для Уникализации Видео

Комплексная система для уникализации видео с Telegram ботом, веб-панелью администратора и базой данных PostgreSQL.

## 🏗️ Архитектура проекта

### Основные компоненты:

1. **🤖 Telegram Бот** (`bot.py`) - Основной интерфейс для пользователей
2. **🎬 Модуль обработки видео** (`video_processor.py`) - Алгоритмы уникализации
3. **🗄️ База данных PostgreSQL** (`database_postgres.py`) - Хранение данных и статистики
4. **🌐 Админ-панель** (`admin_panel.py`) - Веб-интерфейс для управления
5. **⚙️ Конфигурация** (`config.py`) - Настройки системы

## 🚀 Функционал

### Telegram Бот:
- **Приветствие пользователей** при запуске команды `/start`
- **Загрузка видео** от пользователей (до 50 МБ)
- **Пошаговый выбор параметров обработки:**
  - Количество копий (1-3)
  - Добавление цветных рамок
  - Изменение разрешения (или сохранение оригинального)
  - Сжатие видео (включая lossless режим)
- **⚡ Параллельная обработка** всех копий одновременно
- **🔒 Ограничение нагрузки** - максимум 5 одновременных обработок
- **Поддержка множественных пользователей** одновременно (до 50+)
- **Автоматическая очистка** временных файлов

### Админ-панель:
- **🔐 Система авторизации** для администраторов
- **📊 Статистика использования** в реальном времени
- **👥 Управление пользователями** (просмотр, бан, разбан)
- **🔧 Массовые операции** с пользователями
- **📈 Аналитика активности** пользователей
- **📋 Экспорт данных** в CSV формате

## 🗄️ База данных PostgreSQL

### Реализованные таблицы:

#### `users` - Пользователи системы
- `user_id` (BIGINT, PRIMARY KEY) - ID пользователя Telegram
- `username` (VARCHAR) - Имя пользователя
- `first_name` (VARCHAR) - Имя
- `last_name` (VARCHAR) - Фамилия
- `is_banned` (BOOLEAN) - Статус блокировки
- `videos_processed` (INTEGER) - Количество обработанных видео
- `created_at` (TIMESTAMP) - Дата регистрации
- `last_activity` (TIMESTAMP) - Последняя активность

#### `user_activity` - Логирование активности
- `id` (SERIAL, PRIMARY KEY) - Уникальный ID записи
- `user_id` (BIGINT) - ID пользователя
- `action` (VARCHAR) - Тип действия
- `timestamp` (TIMESTAMP) - Время действия
- `details` (TEXT) - Дополнительная информация

#### `admins` - Администраторы системы
- `id` (SERIAL, PRIMARY KEY) - Уникальный ID
- `user_id` (BIGINT) - ID пользователя Telegram
- `username` (VARCHAR) - Имя пользователя
- `role` (VARCHAR) - Роль (admin/super_admin)
- `created_at` (TIMESTAMP) - Дата назначения
- `created_by` (BIGINT) - Кто назначил

#### `admin_actions` - Журнал действий администраторов
- `id` (SERIAL, PRIMARY KEY) - Уникальный ID
- `admin_id` (BIGINT) - ID администратора
- `action` (VARCHAR) - Тип действия
- `target_user_id` (BIGINT) - ID целевого пользователя
- `reason` (TEXT) - Причина действия
- `timestamp` (TIMESTAMP) - Время действия

#### `daily_stats` - Ежедневная статистика
- `date` (DATE, PRIMARY KEY) - Дата
- `new_users` (INTEGER) - Новые пользователи
- `active_users` (INTEGER) - Активные пользователи
- `videos_processed` (INTEGER) - Обработано видео
- `total_users` (INTEGER) - Общее количество пользователей

### Реализованные функции базы данных:

#### ✅ Управление пользователями:
- `add_or_update_user()` - Добавление/обновление пользователя
- `get_user_details()` - Получение информации о пользователе
- `ban_user()` / `unban_user()` - Блокировка/разблокировка
- `is_user_banned()` - Проверка статуса блокировки
- `increment_user_videos()` - Увеличение счетчика видео

#### ✅ Система администрирования:
- `is_admin()` / `is_super_admin()` - Проверка прав доступа
- `set_admin_status()` - Назначение/снятие прав администратора
- `get_all_admins()` - Список всех администраторов
- `create_first_admin()` - Создание первого супер-администратора

#### ✅ Статистика и аналитика:
- `get_total_users()` - Общее количество пользователей
- `get_active_users_today()` - Активные пользователи за сегодня
- `get_videos_processed_today()` - Обработано видео за сегодня
- `get_user_activity()` - История активности пользователя
- `log_user_activity()` - Логирование действий пользователя

#### ✅ Поиск и фильтрация:
- `search_users()` - Поиск пользователей по критериям
- `get_users_with_filters()` - Получение пользователей с фильтрами
- `get_banned_users()` - Список заблокированных пользователей

### ❌ Не реализованные функции:
- Система уведомлений администраторов
- Автоматическое резервное копирование
- Система ролей с детальными правами
- Интеграция с внешними аналитическими системами
- Система отчетов по расписанию

## 🎬 Алгоритмы уникализации видео

### Реализованные методы:

#### 1. **Цветные рамки** (`add_frame_to_video`)
- 6 различных цветов рамок
- Случайная толщина рамок
- Позиционирование: верх, низ, лево, право

#### 2. **Параметры сжатия** (`generate_random_compression_settings`)
- **CRF (Constant Rate Factor)**: 0-51
- **Lossless режим**: 30% шанс на CRF=0 (без потерь качества)
- **Preset**: скорость кодирования (ultrafast, superfast, veryfast, faster, fast, medium)
- **Movflags**: оптимизация для веб-воспроизведения

#### 3. **Изменение разрешения и FPS**
- Случайное масштабирование видео
- Изменение частоты кадров
- Поддержка различных соотношений сторон

#### 4. **Дополнительные параметры**
- Изменение битрейта (500k-2000k)
- Настройка качества кодирования
- Оптимизация для различных платформ

### Производительность:
- **1 копия:** ~2-3 минуты
- **2 копии:** ~2-3 минуты (параллельная обработка!)
- **3 копии:** ~2-3 минуты (в 3 раза быстрее последовательной обработки!)

## 📋 Требования

- Python 3.8+
- PostgreSQL 12+
- Telegram Bot Token (получить у [@BotFather](https://t.me/botfather))
- FFmpeg (для обработки видео)

## 🛠 Установка

1. **Клонируйте репозиторий:**
   ```bash
   git clone <repository_url>
   cd finnaly_videoBot
   ```

2. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

3. **Настройте PostgreSQL:**
   ```bash
   # Создайте базу данных
   createdb videobot_db
   
   # Или используйте скрипт настройки
   python setup_postgres.py
   ```

4. **Настройте переменные окружения:**
   ```bash
   copy .env.example .env
   ```
   
   Отредактируйте `.env`:
   ```env
   # Telegram Bot
   BOT_TOKEN=your_telegram_bot_token
   
   # PostgreSQL Database
   DB_HOST=localhost
   DB_PORT=5432
   DB_NAME=videobot_db
   DB_USER=your_db_user
   DB_PASSWORD=your_db_password
   
   # Admin Panel
   ADMIN_USERNAME=admin
   ADMIN_PASSWORD=your_admin_password
   ```

## 🎯 Запуск

### Telegram Бот:
```bash
python bot.py
```

### Админ-панель:
```bash
python admin_panel.py
```
Доступ: http://localhost:5000

## 🧪 Тестирование

### 1. Проверка окружения:
```bash
python test_local.py
```

### 2. Тестирование компонентов:

#### База данных:
```python
from database_postgres import DatabaseManager
db = DatabaseManager()
db.connect()
print("✅ База данных подключена")
```

#### Обработка видео:
```python
from video_processor import VideoProcessor
processor = VideoProcessor()
settings = processor.generate_random_compression_settings()
print(f"Lossless: {settings['crf'] == 0}")
```

#### Админ-панель:
- Откройте http://localhost:5000
- Войдите с учетными данными из `.env`
- Проверьте статистику и управление пользователями

### 3. Интеграционное тестирование:
1. Запустите бота: `python bot.py`
2. Отправьте команду `/start` в Telegram
3. Загрузите тестовое видео
4. Проверьте обработку и результат
5. Проверьте статистику в админ-панели

## 📁 Структура проекта

```
finnaly_videoBot/
├── bot.py                    # Telegram бот
├── video_processor.py        # Обработка видео
├── database_postgres.py      # PostgreSQL интеграция
├── admin_panel.py           # Flask веб-панель
├── config.py                # Конфигурация
├── test_local.py            # Локальное тестирование
├── setup_postgres.py        # Настройка БД
├── view_database.py         # Просмотр данных БД
├── requirements.txt         # Python зависимости
├── .env.example            # Пример переменных окружения
├── static/                 # Статические файлы админ-панели
│   ├── css/
│   └── js/
├── templates/              # HTML шаблоны
│   ├── base.html
│   ├── dashboard.html
│   ├── login.html
│   └── users.html
├── processed_videos/       # Обработанные видео
└── temp/                  # Временные файлы
```

## 🎮 Использование

### Telegram Бот:
1. Запустите бота командой `/start`
2. Отправьте видеофайл боту
3. Нажмите "🎬 Уникализировать видео"
4. Выберите параметры обработки
5. Получите уникализированные копии

### Админ-панель:
1. Откройте http://localhost:5000
2. Войдите с учетными данными администратора
3. Просматривайте статистику и управляйте пользователями
4. Используйте массовые операции для эффективного управления

## ⚙️ Конфигурация

### Поддерживаемые форматы:
- MP4, AVI, MOV, MKV

### Ограничения:
- Максимальный размер файла: 50 MB
- Максимум 3 копии за раз
- Максимум 5 одновременных обработок

### Настройки в `config.py`:
- `MAX_VIDEO_SIZE` - максимальный размер видео
- `SUPPORTED_FORMATS` - поддерживаемые форматы
- `OUTPUT_DIR` - папка для результатов
- `TEMP_DIR` - временные файлы
- `MAX_CONCURRENT_PROCESSES` - лимит обработок

## 🔧 Устранение неполадок

### База данных:
```bash
# Проверка подключения
python -c "from database_postgres import DatabaseManager; db = DatabaseManager(); db.connect()"

# Просмотр данных
python view_database.py
```

### Telegram бот:
- Проверьте токен в `.env`
- Убедитесь в доступности PostgreSQL
- Проверьте логи в консоли

### Админ-панель:
- Проверьте учетные данные в `.env`
- Убедитесь, что порт 5000 свободен
- Проверьте подключение к базе данных

## 🖥️ Требования к серверу

### Рекомендуемая конфигурация:
```
CPU: 16 vCPU
RAM: 32 ГБ
SSD: 100 ГБ
PostgreSQL: 12+
Стоимость: ~₽10,000/мес
```

### Минимальная конфигурация:
```
CPU: 8 vCPU
RAM: 16 ГБ
SSD: 50 ГБ
PostgreSQL: 12+
Стоимость: ~₽6,000/мес
```

## 🔒 Безопасность

- Автоматическая очистка временных файлов
- Изолированное пространство для каждого пользователя
- Система ролей администраторов
- Логирование всех действий
- Защита от SQL-инъекций через SQLAlchemy

---

**Автор:** Nikita  
**Версия:** 3.0.0 (с PostgreSQL и админ-панелью)  
**Лицензия:** MIT  
**Дата обновления:** 2025-01-25
